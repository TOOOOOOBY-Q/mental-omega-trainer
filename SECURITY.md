# 安全政策

## 支持版本

| 版本 | 状态 |
| --- | --- |
| 3.3.6.4 | 当前支持 |
| < 3.3.6.4 | 不再维护 |

## 程序行为说明

本工具属于单机游戏修改器，其正常工作依赖以下高权限行为，杀毒软件或 SmartScreen 可能因此提示：

- 以管理员权限运行（打开游戏进程句柄所需）
- 对 `gamemd.exe` 执行内存读写（`ReadProcessMemory` / `WriteProcessMemory`）
- 一键全图功能在游戏进程内创建远程线程，调用游戏自身的地图揭开函数

程序不联网、不读写游戏目录文件、不包含自启动或持久化逻辑。

## 发布与校验

- 发布版本使用 `CN=TOOOOOOBY` 自签名证书签名；自签名不属于公共 CA 信任链，Windows 提示证书不受信任属预期现象。
- 每个 Release 附带 SHA-256 校验值，下载后请核对。
- 签名私钥与证书文件（.pfx/.cer）不进入本仓库。

## 报告安全问题

请通过 GitHub Issues 报告安全问题。不要公开披露可能导致滥用的细节，也不要在 Issue 中提交任何个人凭据、账号信息或私密路径。
